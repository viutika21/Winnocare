import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ase.winnocare',
  appName: 'Winnocare',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
